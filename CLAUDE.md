# 專案規則（CLAUDE.md）

本檔為 Claude Code 的專案級常駐指示。每次於本 repo 啟動時請遵循以下規則。

## 專案概況

此 repo（Jack6349/myweb）部署於 GitHub Pages，根目錄下平鋪多個獨立 App（travel、dividend、信用卡總管、股利總管等）。各 App 為獨立的靜態網頁。

## 開發約束（重要）

- **不得更動既有上線中的 App**，特別是 `/travel` 目錄（舊版旅行 App，線上運作中）。
- 重大檔案操作（移動、刪除、覆蓋既有檔案）前**務必先提示說明**，等使用者確認後再執行。
- 動手開發前，**先說明實作計畫，等使用者確認後才建立或修改檔案**。
- 所有網頁資源使用**相對路徑**，禁止硬寫絕對路徑（如 `/myweb/...`）。

## 目錄與文件管理規則

- 每個 App 的開發文件統一放在 `_docs/<app名稱>/` 之下，例如 `_docs/travel-v2/`。
- 文件按 App 分子目錄存放，不可全部平鋪於 `_docs/` 單層。
- `_docs/` 為開發內部文件，**不同步至 GitHub**（已由 .gitignore 排除）。
- 維護目錄結構時依本規則執行，不自行更動結構慣例。

## 當前開發任務

- 目標：在 `travel-v2/` 目錄開發新版旅行行程 App 的 **可拋棄式 UI Prototype**。
- 規格文件：`_docs/travel-v2/prototype-brief.md`。
- Prototype 階段：純畫面、不接真實資料、不寫功能邏輯、不接後端，資料用假資料佔位。
- 開發方式：先建 UI/動線 → 使用者親手操作驗證 → 再依畫面反推資料需求 → 最後才接功能與後端。
